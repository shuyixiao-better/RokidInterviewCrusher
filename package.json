{
  "name": "rokid-interview-crusher",
  "version": "1.0.0",
  "description": "基于 Rokid 智能眼镜的 AI 面试训练与复盘辅助工具",
  "type": "module",
  "main": "app.js",
  "scripts": {
    "build": "rokid-cli build || echo 'rokid-cli not available'",
    "deploy": "rokid-cli install dist/*.aix || echo 'rokid-cli not available'",
    "lint": "eslint src/ --ext .js",
    "lint:fix": "eslint src/ --ext .js --fix"
  },
  "keywords": [
    "rokid",
    "aiui",
    "interview",
    "ai",
    "smart-glasses"
  ],
  "author": "RokidInterviewCrusher Team",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
